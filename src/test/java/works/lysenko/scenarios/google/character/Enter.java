package works.lysenko.scenarios.google.character;

import works.lysenko.Execution;

@SuppressWarnings("javadoc")
public class Enter extends works.lysenko.common.Enter {
	public Enter(Execution x) {
		super(x);
	}
}